#install apache2
---

- hosts: all
  become: true
  tasks:

    - name: update repository index
      apt:
        update_cache: yes

    - name: install apache2 pkg
      apt:
        name: apache2

# to run: ansible-playbook --ask-become-pass apache2_playbook.yml